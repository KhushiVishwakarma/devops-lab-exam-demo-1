# Factorial CLI

Simple Python application to compute factorials.

Usage

Run from the command line:

```
python3 factorial.py 5
```

This prints `5! = 120`.

To print only the number:

```
python3 factorial.py -q 5
```

Run unit tests:

```
python3 -m unittest -v
```
